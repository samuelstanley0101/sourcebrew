# Sourcebrew

A package manager inspired by [Homebrew](https://brew.sh/) for compiling and installing applications from the source code on macOS, since the `--build-from-source` flag in `brew install` never seems to work for me.
